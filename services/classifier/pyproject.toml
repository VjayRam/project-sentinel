[project]
name = "sentinel-classifier"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "transformers>=4.48",
    "torch>=2.2",
    "prometheus-client>=0.21",
    "pydantic>=2.10",
]
