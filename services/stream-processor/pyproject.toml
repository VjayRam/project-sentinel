[project]
name = "sentinel-stream-processor"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "kafka-python>=2.0",
    "httpx>=0.27",
    "pymongo>=4.0",
    "psycopg[binary]>=3.2",
]
